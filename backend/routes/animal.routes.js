/* 
const express = require('express');
const animalControl = require('../controller/animal.controller');
const api1 = express.Router();


api1.get('/cualquiera', (req, res)=>{res.send('mensaje')});
api1.post('/crear1', animalControl.crearAnimal);

api1.get('/mostrar1', animalControl.obtenerAnimal);

module.exports = api1;
 */